# SEO / Performance Report — `<URL>`

**Source:** PageSpeed Insights (<https://pagespeed.web.dev/>) · **Date:** `<date>` ·
**Strategy:** Mobile + Desktop

## Scores (0–100)
| Category | Mobile | Desktop |
|----------|:------:|:-------:|
| Performance | | |
| Accessibility | | |
| Best Practices | | |
| SEO | | |

---

## 1. Core Web Vitals (field / real users)
| Metric | Value | Target | Status |
|--------|-------|--------|--------|
| LCP | | < 2.5s | |
| INP | | < 200ms | |
| CLS | | < 0.1 | |
| FCP | | < 1.8s | |
| TTFB | | < 0.8s | |

**Overall CWV assessment:** Passed / Failed — _why + what to fix_.

## 2. Performance (lab metrics)
| Metric | Value | Rating |
|--------|-------|--------|
| First Contentful Paint | | |
| Largest Contentful Paint | | |
| Total Blocking Time | | |
| Cumulative Layout Shift | | |
| Speed Index | | |

## 3. Opportunities (biggest wins)
For each PSI opportunity — **Finding → Estimated savings → Recommended fix**:
- **`<opportunity>`** — est. `~X.Xs` / `XX KB` — _fix_.

## 4. Diagnostics
For each diagnostic — **Finding → Impact → Fix**:
- **`<diagnostic>`** — _impact_ — _fix_.

## 5. Accessibility
Score + failed audits, each **Finding → Fix** (contrast, labels, alt, roles, …).

## 6. Best Practices
Score + failed audits, each **Finding → Fix** (HTTPS, console errors, image
aspect ratio, deprecated APIs, …).

## 7. SEO
Score + failed audits, each **Finding → Fix** (indexable / `robots`, `title` +
meta description, crawlable links, valid `hreflang`, tap targets, structured
data, …).

---

## Prioritized action list
Highest impact / lowest effort first:
1. **`<action>`** — _expected gain_ — owner: `seo`/`frontend`.
2. …

> Report **measured** numbers from PSI, never assumed. Re-run PSI after fixes to
> confirm the deltas.
