# Architecture — MongoDB

Let access patterns drive the schema. Embed what is read together; reference what is large/shared/independently updated. Bound growth; shard on a key that spreads writes and routes reads.
