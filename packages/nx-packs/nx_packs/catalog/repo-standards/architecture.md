# Architecture — Repository Standards

A conventional layout: source +
tests + docs, a `.github/` with CI and templates, governance files at the root, and
tag-driven release automation. `nxai scaffold` provisions this skeleton; teams adapt it.
