-- Non-secret reference/sample data for {{project_title}}.
-- Safe to run in any environment; contains no secrets. Extend as needed.
-- (Intentionally empty of business data — this is a foundation.)

-- Example (commented): seed a demo item for a known owner.
-- insert into public.items (owner_id, name)
-- values ('00000000-0000-0000-0000-000000000000', 'demo')
-- on conflict do nothing;
